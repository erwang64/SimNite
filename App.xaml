<Application x:Class="SimNite.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:viewModels="clr-namespace:SimNite.ViewModels"
             xmlns:views="clr-namespace:SimNite.Views"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- Dark Theme Colors -->
        <SolidColorBrush x:Key="SimNiteBackgroundBrush" Color="#0F0F0F" />
        <SolidColorBrush x:Key="SimNiteSurfaceBrush" Color="#1A1A1A" />
        <SolidColorBrush x:Key="SimNitePanelBrush" Color="#242424" />
        <SolidColorBrush x:Key="SimNiteBorderBrush" Color="#333333" />
        <SolidColorBrush x:Key="SimNitePrimaryBrush" Color="#F7C614" />
        <SolidColorBrush x:Key="SimNiteTextBrush" Color="#E8E8E8" />
        <SolidColorBrush x:Key="SimNiteMutedBrush" Color="#999999" />
        <SolidColorBrush x:Key="SimNiteErrorBrush" Color="#EF4444" />
        <SolidColorBrush x:Key="SimNiteSuccessBrush" Color="#10B981" />
        <SolidColorBrush x:Key="SimNiteHoverBrush" Color="#2A2A2A" />
        <SolidColorBrush x:Key="SimNiteInputBrush" Color="#1E1E1E" />

        <Style TargetType="Window">
            <Setter Property="Background" Value="{StaticResource SimNiteBackgroundBrush}" />
            <Setter Property="Foreground" Value="{StaticResource SimNiteTextBrush}" />
            <Setter Property="FontFamily" Value="Segoe UI" />
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource SimNiteTextBrush}" />
        </Style>

        <Style x:Key="TitleTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="28" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Foreground" Value="{StaticResource SimNiteTextBrush}" />
        </Style>

        <Style x:Key="SectionTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{StaticResource SimNiteTextBrush}" />
        </Style>

        <Style TargetType="Border">
            <Setter Property="CornerRadius" Value="14" />
            <Setter Property="BorderBrush" Value="{StaticResource SimNiteBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource SimNiteTextBrush}" />
            <Setter Property="Background" Value="#2A2A2A" />
            <Setter Property="BorderBrush" Value="{StaticResource SimNiteBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="14,8" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="buttonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                SnapsToDevicePixels="True">
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="buttonBorder" Property="Background" Value="#383838" />
                                <Setter TargetName="buttonBorder" Property="BorderBrush" Value="{StaticResource SimNitePrimaryBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="buttonBorder" Property="Background" Value="#424242" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="buttonBorder" Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource SimNitePrimaryBrush}" />
            <Setter Property="Foreground" Value="#000000" />
            <Setter Property="BorderBrush" Value="#D6AA10" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style x:Key="NavButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Padding" Value="16,8" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Background" Value="#2F2F2F" />
            <Setter Property="BorderBrush" Value="#444444" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource SimNiteInputBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource SimNiteBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Foreground" Value="{StaticResource SimNiteTextBrush}" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="CaretBrush" Value="{StaticResource SimNitePrimaryBrush}" />
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource SimNiteInputBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource SimNiteBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="6,4" />
            <Setter Property="Foreground" Value="{StaticResource SimNiteTextBrush}" />
        </Style>

        <Style TargetType="ProgressBar">
            <Setter Property="Foreground" Value="{StaticResource SimNitePrimaryBrush}" />
            <Setter Property="Background" Value="#2A2A2A" />
            <Setter Property="Height" Value="20" />
            <Setter Property="BorderBrush" Value="{StaticResource SimNiteBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="{StaticResource SimNiteInputBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource SimNiteBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="GridLinesVisibility" Value="Horizontal" />
            <Setter Property="HorizontalGridLinesBrush" Value="#2A2A2A" />
            <Setter Property="VerticalGridLinesBrush" Value="#2A2A2A" />
            <Setter Property="RowBackground" Value="{StaticResource SimNiteInputBrush}" />
            <Setter Property="AlternatingRowBackground" Value="#242424" />
            <Setter Property="HeadersVisibility" Value="Column" />
            <Setter Property="Foreground" Value="{StaticResource SimNiteTextBrush}" />
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#2F2F2F" />
            <Setter Property="Foreground" Value="{StaticResource SimNiteTextBrush}" />
            <Setter Property="BorderBrush" Value="#3A3A3A" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Foreground" Value="{StaticResource SimNiteTextBrush}" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>

        <DataTemplate DataType="{x:Type viewModels:ModListViewModel}">
            <views:ModListView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:InstallViewModel}">
            <views:InstallView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:ProfileViewModel}">
            <views:ProfileView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:SettingsViewModel}">
            <views:SettingsView />
        </DataTemplate>

    </Application.Resources>
</Application>
