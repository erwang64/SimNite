<UserControl x:Class="SimNite.Views.ProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Margin="6,0,0,12" Style="{StaticResource TitleTextStyle}" Text="Profiles" />

        <Grid Grid.Row="1" Margin="6,0,6,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="20" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Export Profile Card -->
            <Border Grid.Column="0" Padding="24" Background="{StaticResource SimNitePanelBrush}" CornerRadius="12">
                <StackPanel>
                    <TextBlock Text="Export Profile" Foreground="{StaticResource SimNiteTextBrush}" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,8" />
                    <TextBlock Text="Save your current mod selection to a JSON file." Foreground="{StaticResource SimNiteMutedBrush}" TextWrapping="Wrap" Margin="0,0,0,24" />

                    <TextBlock Text="Profile Name" Foreground="{StaticResource SimNiteMutedBrush}" Margin="0,0,0,4" />
                    <Border Background="{StaticResource SimNiteInputBrush}" BorderBrush="{StaticResource SimNiteBorderBrush}" BorderThickness="1" CornerRadius="6" Height="40" Margin="0,0,0,16">
                        <TextBox Background="Transparent" BorderThickness="0" Foreground="{StaticResource SimNiteTextBrush}" VerticalContentAlignment="Center" Padding="12,0" Text="{Binding ProfileName, UpdateSourceTrigger=PropertyChanged}" />
                    </Border>

                    <TextBlock Text="Save Location" Foreground="{StaticResource SimNiteMutedBrush}" Margin="0,0,0,4" />
                    <Grid Margin="0,0,0,24">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Background="{StaticResource SimNiteInputBrush}" BorderBrush="{StaticResource SimNiteBorderBrush}" BorderThickness="1" CornerRadius="6,0,0,6" Height="40">
                            <TextBox Background="Transparent" BorderThickness="0" Foreground="{StaticResource SimNiteTextBrush}" VerticalContentAlignment="Center" Padding="12,0" Text="{Binding ProfilePath, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True" />
                        </Border>
                        <Button Grid.Column="1" Command="{Binding BrowseSaveProfilePathCommand}" Content="📁 Browse" Background="{StaticResource SimNiteInputHoverBrush}" BorderBrush="{StaticResource SimNiteBorderBrush}" BorderThickness="0,1,1,1" Foreground="{StaticResource SimNiteTextBrush}" Padding="16,0" Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="0,6,6,0">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </Grid>

                    <Button Style="{StaticResource PrimaryButtonStyle}" Command="{Binding SaveProfileCommand}" Content="Save Profile" Height="40" FontSize="14" />
                </StackPanel>
            </Border>

            <!-- Import Profile Card -->
            <Border Grid.Column="2" Padding="24" Background="{StaticResource SimNitePanelBrush}" CornerRadius="12">
                <StackPanel>
                    <TextBlock Text="Import Profile" Foreground="{StaticResource SimNiteTextBrush}" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,8" />
                    <TextBlock Text="Load an existing profile to restore your mod list." Foreground="{StaticResource SimNiteMutedBrush}" TextWrapping="Wrap" Margin="0,0,0,24" />

                    <TextBlock Text="Profile File" Foreground="{StaticResource SimNiteMutedBrush}" Margin="0,0,0,4" />
                    <Grid Margin="0,0,0,24">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Background="{StaticResource SimNiteInputBrush}" BorderBrush="{StaticResource SimNiteBorderBrush}" BorderThickness="1" CornerRadius="6,0,0,6" Height="40">
                            <TextBox Background="Transparent" BorderThickness="0" Foreground="{StaticResource SimNiteTextBrush}" VerticalContentAlignment="Center" Padding="12,0" Text="{Binding ProfilePath, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True" />
                        </Border>
                        <Button Grid.Column="1" Command="{Binding BrowseProfilePathCommand}" Content="📁 Browse" Background="{StaticResource SimNiteInputHoverBrush}" BorderBrush="{StaticResource SimNiteBorderBrush}" BorderThickness="0,1,1,1" Foreground="{StaticResource SimNiteTextBrush}" Padding="16,0" Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="0,6,6,0">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </Grid>

                    <Button Command="{Binding LoadProfileCommand}" Content="Load Profile" Height="40" FontSize="14" Background="{StaticResource SimNiteInputBrush}" BorderBrush="{StaticResource SimNiteBorderBrush}" Foreground="{StaticResource SimNiteTextBrush}" Margin="0,0,0,32" />

                    <TextBlock Text="Community Folder Check" Foreground="{StaticResource SimNiteTextBrush}" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,8" />
                    <TextBlock Text="Scan Community Folder to check which mods are installed." Foreground="{StaticResource SimNiteMutedBrush}" TextWrapping="Wrap" Margin="0,0,0,16" />
                    <Button Command="{Binding ScanCommunityCommand}" Content="Scan Community Folder" Height="40" FontSize="14" Background="{StaticResource SimNiteInputBrush}" BorderBrush="{StaticResource SimNiteBorderBrush}" Foreground="{StaticResource SimNiteTextBrush}" />
                </StackPanel>
            </Border>
        </Grid>

        <TextBlock Grid.Row="1"
                   Margin="16,0,16,16"
                   VerticalAlignment="Bottom"
                   Foreground="{StaticResource SimNiteSuccessBrush}"
                   FontWeight="SemiBold"
                   Text="{Binding StatusMessage}" />
    </Grid>
</UserControl>

