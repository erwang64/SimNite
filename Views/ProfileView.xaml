<UserControl x:Class="SimNite.Views.ProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="180" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>

    <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Margin="0,0,0,14" FontSize="22" FontWeight="SemiBold" Text="Profils" />

    <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,12,8" VerticalAlignment="Center" Text="Nom du profil" />
    <TextBox Grid.Row="1" Grid.Column="1" Height="30" Margin="0,0,0,8" Text="{Binding ProfileName, UpdateSourceTrigger=PropertyChanged}" />

    <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,0,12,8" VerticalAlignment="Center" Text="Fichier profil" />
    <TextBox Grid.Row="2" Grid.Column="1" Height="30" Margin="0,0,0,8" Text="{Binding ProfilePath, UpdateSourceTrigger=PropertyChanged}" />

    <TextBlock Grid.Row="3" Grid.Column="0" Margin="0,0,12,8" VerticalAlignment="Center" Text="Community Folder" />
    <TextBox Grid.Row="3" Grid.Column="1" Height="30" Margin="0,0,0,8" Text="{Binding CommunityFolderPath, UpdateSourceTrigger=PropertyChanged}" />

    <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,4,0,8">
      <Button Margin="0,0,8,0" Padding="12,6" Command="{Binding SaveProfileCommand}" Content="Sauvegarder" />
      <Button Margin="0,0,8,0" Padding="12,6" Command="{Binding LoadProfileCommand}" Content="Charger" />
      <Button Padding="12,6" Command="{Binding ScanCommunityCommand}" Content="Scanner Community" />
    </StackPanel>

    <TextBlock Grid.Row="5" Grid.ColumnSpan="2" Foreground="#1E3A8A" FontWeight="SemiBold" Text="{Binding StatusMessage}" />
  </Grid>
</UserControl>

