<UserControl x:Class="SimNite.Views.InstallView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Style="{StaticResource TitleTextStyle}" Text="Progression d'installation" />

        <Border Grid.Row="1" Margin="0,12,0,10" Padding="14" Background="{StaticResource SimNitePanelBrush}">
            <StackPanel>
                <TextBlock FontWeight="SemiBold" Text="Etape en cours" />
                <TextBlock Margin="0,4,0,0" FontSize="14" Foreground="{StaticResource SimNiteMutedBrush}" Text="{Binding CurrentStep}" />
            </StackPanel>
        </Border>

        <ProgressBar Grid.Row="2"
                     Height="26"
                     Maximum="100"
                     Minimum="0"
                     Value="{Binding OverallProgress}" />

        <StackPanel Grid.Row="3" Margin="0,12,0,0">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <Button Margin="0,0,8,0" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding StartSimulationCommand}" Content="Demarrer installation" />
                <Button Command="{Binding ResetCommand}" Content="Reset" />
            </StackPanel>

            <Border Background="{StaticResource SimNiteSurfaceBrush}" Padding="0">
                <DataGrid AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          ItemsSource="{Binding Tasks}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Width="2*" Header="Mod" Binding="{Binding Mod.Name}" />
                        <DataGridTextColumn Width="*" Header="Statut" Binding="{Binding Status}" />
                        <DataGridTextColumn Width="*" Header="Progression" Binding="{Binding Progress}" />
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>

