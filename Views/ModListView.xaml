<UserControl x:Class="SimNite.Views.ModListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="220" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0"
                   Grid.ColumnSpan="3"
                   Margin="0,0,0,12"
                   FontSize="22"
                   FontWeight="SemiBold"
                   Text="Catalogue des mods" />

        <TextBox Grid.Row="1"
                 Grid.Column="0"
                 Height="32"
                 Margin="0,0,12,12"
                 VerticalContentAlignment="Center"
                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />

        <ComboBox Grid.Row="1"
                  Grid.Column="1"
                  Height="32"
                  Margin="0,0,12,12"
                  ItemsSource="{Binding Categories}"
                  SelectedItem="{Binding SelectedCategory}" />

        <StackPanel Grid.Row="1" Grid.Column="2" Orientation="Horizontal">
            <Button Margin="0,0,8,12" Padding="10,6" Command="{Binding LoadModsCommand}" Content="Charger" />
            <Button Margin="0,0,8,12" Padding="10,6" Command="{Binding RefreshModsCommand}" Content="Rafraichir" />
            <Button Margin="0,0,8,12" Padding="10,6" Command="{Binding ClearFiltersCommand}" Content="Effacer" />
            <Button Margin="0,0,0,12" Padding="10,6" Command="{Binding CancelLoadCommand}" Content="Annuler" />
        </StackPanel>

        <Grid Grid.Row="2" Grid.ColumnSpan="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Margin="0,0,0,8"
                       Foreground="#B91C1C"
                       FontWeight="SemiBold"
                       Text="{Binding ErrorMessage}" />

            <DataGrid Grid.Row="1"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      HeadersVisibility="Column"
                      IsReadOnly="False"
                      ItemsSource="{Binding FilteredMods}">
                <DataGrid.Columns>
                    <DataGridCheckBoxColumn Width="70" Header="Use" Binding="{Binding IsChecked}" />
                    <DataGridTextColumn Width="2*" Header="Name" Binding="{Binding Name}" IsReadOnly="True" />
                    <DataGridTextColumn Width="*" Header="Category" Binding="{Binding Category}" IsReadOnly="True" />
                    <DataGridTextColumn Width="*" Header="Type" Binding="{Binding Type}" IsReadOnly="True" />
                    <DataGridTextColumn Width="*" Header="Mode" Binding="{Binding InstallMode}" IsReadOnly="True" />
                    <DataGridTextColumn Width="*" Header="Version" Binding="{Binding Version}" IsReadOnly="True" />
                    <DataGridTextColumn Width="*" Header="Size (bytes)" Binding="{Binding SizeBytes}" IsReadOnly="True" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>

