<UserControl x:Class="SimNite.Views.WelcomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{StaticResource SimNiteBackgroundBrush}">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Welcome to SimNite Manager" FontSize="36" FontWeight="Bold" Foreground="{StaticResource SimNiteTextBrush}" HorizontalAlignment="Center" Margin="0,0,0,16"/>
            <TextBlock Text="Select 'Mods' on the left menu to continue." FontSize="16" Foreground="{StaticResource SimNiteMutedBrush}" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
