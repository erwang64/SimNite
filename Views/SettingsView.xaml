<UserControl x:Class="SimNite.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Margin="6,0,0,12" Style="{StaticResource TitleTextStyle}" Text="Parametres" />

        <Border Grid.Row="1" Padding="16" Background="{StaticResource SimNitePanelBrush}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="220" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,12,8" VerticalAlignment="Center" Foreground="{StaticResource SimNiteMutedBrush}" Text="Fichier settings" />
                <TextBox Grid.Row="0" Grid.Column="1" Height="36" Margin="0,0,0,8" Text="{Binding SettingsPath, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,12,8" VerticalAlignment="Center" Foreground="{StaticResource SimNiteMutedBrush}" Text="Community Folder" />
                <TextBox Grid.Row="1" Grid.Column="1" Height="36" Margin="0,0,0,8" Text="{Binding CommunityFolderPath, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,0,12,8" VerticalAlignment="Center" Foreground="{StaticResource SimNiteMutedBrush}" Text="Download temp path" />
                <TextBox Grid.Row="2" Grid.Column="1" Height="36" Margin="0,0,0,8" Text="{Binding DownloadTempPath, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Grid.Row="3" Grid.Column="0" Margin="0,0,12,8" VerticalAlignment="Center" Foreground="{StaticResource SimNiteMutedBrush}" Text="Max downloads" />
                <TextBox Grid.Row="3" Grid.Column="1" Height="36" Margin="0,0,0,8" Text="{Binding MaxConcurrentDownloads, UpdateSourceTrigger=PropertyChanged}" />

                <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,6,0,8">
                    <Button Margin="0,0,8,0" Command="{Binding LoadSettingsCommand}" Content="Charger" />
                    <Button Margin="0,0,8,0" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding SaveSettingsCommand}" Content="Sauvegarder" />
                    <Button Command="{Binding DetectCommunityFolderCommand}" Content="Detecter Community" />
                </StackPanel>

                <TextBlock Grid.Row="5"
                           Grid.ColumnSpan="2"
                           Foreground="{StaticResource SimNiteSuccessBrush}"
                           FontWeight="SemiBold"
                           Text="{Binding StatusMessage}" />
            </Grid>
        </Border>
    </Grid>
</UserControl>

